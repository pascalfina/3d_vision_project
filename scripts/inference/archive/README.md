# Archived Inference Wrappers

This folder contains older direct inference wrappers that are no longer the recommended entrypoint.

Use instead:

- `scripts/inference/pipeline/run_pipeline_slat_tmp.sh`
- `scripts/inference/pipeline/run_pipeline_u3dgs_tmp.sh`
- or the profile-based workflow wrapper `scripts/workflows/run_scene_profile.sh`
