# Archived Voxelisation Wrappers

This folder contains older direct voxelisation wrappers kept only for reference.

Use instead:

- `scripts/voxel_annotations/pipeline/voxelise_features_tmp.sh`
- or the profile-based workflow wrapper `scripts/workflows/run_scene_profile.sh`
