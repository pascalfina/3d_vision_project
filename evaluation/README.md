# Evaluation

This directory contains standalone evaluation code for Object-X reconstruction
experiments.  Keep geometry and segmentation metrics separate:

- `geometry/`: scene geometry quality without object labels.
- Future `segmentation/`: object/instance metrics once geometry evaluation is stable.

